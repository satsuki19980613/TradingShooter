/**
 * クライアントサイドのチューニング用パラメーター
 */
export const ClientConfig = {
  // 
  // 描画ループの間隔 (ms)
  // (注: 本来は requestAnimationFrame を使うのがベストプラクティスです)
  // 
  RENDER_LOOP_INTERVAL: 16, // 60fps

  // 
  // 入力（キー操作やマウス位置）をサーバーに送信する間隔 (ms)
  // 応答性（レスポンス）に影響します。
  // 16ms = 60fps (最も応答性が高い)
  // 32ms = 30fps (十分実用的)
  // 
  INPUT_SEND_INTERVAL: 33,

  // 
  // エンティティの「補間レート」(Lerp)
  // サーバーからのカクついた動きを、どれだけ滑らかにするか。
  // 1.0 = 補間なし (サーバー座標に即座にジャンプする)
  // 0.3 = やや速めに追従 (キビキビ動く)
  // 0.1 = ゆっくり追従 (フワフワ動く)
  // 
  CLIENT_LERP_RATE: 0.15,
};