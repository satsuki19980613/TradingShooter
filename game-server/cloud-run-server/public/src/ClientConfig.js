export const ClientConfig = {
  RENDER_LOOP_INTERVAL: 16,
  INPUT_SEND_INTERVAL: 33,
  CLIENT_LERP_RATE: 0.15,

  // ★変更: ここを「18」から「10」や「12」に下げると、画面がズームされキャラが大きくなります
  VIEWPORT_GRID_WIDTH: 8, 
  
  // 1マスのサイズ (変更不要)
  GRID_SIZE: 150, 
};